{
  "name": "homepage",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "deploy": "rm -rf node_modules/.cache && next build && next export && echo ryosuzuki.org > out/CNAME && touch out/.nojekyll && mv out/static/publications out/ && mv out/static/cv/cv.pdf out/ && git add out/",
    "push" : "git subtree push --prefix out origin gh-pages",
    "build:content": "processmd \"content/**/*.{yaml,md}\" --outputDir output --summaryOutput output/summary.json",
    "build:content:watch": "processmd \"content/**/*.{yaml,md}\" --outputDir output --watch"
  },
  "author": "Ryo Suzuki <ryosuzk@googlemail.com> (http://ryosuzuki.org/)",
  "license": "ISC",
  "dependencies": {
    "@zeit/next-css": "^0.2.0",
    "jquery": "^3.3.1",
    "next": "^6.1.1",
    "next-routes": "^1.4.2",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-markdown": "^3.3.4"
  },
  "devDependencies": {
    "processmd": "^4.2.1"
  }
}
